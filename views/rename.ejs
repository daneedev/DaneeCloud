<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= cloudname %> | <%= lang["Rename"] %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
</head>
<body>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@500&display=swap');

        body {
            background-image: linear-gradient(to right bottom, #1f74cc, #3c7ed1, #5087d6, #6291db, #729be0, #87a6e4, #9ab1e9, #acbced, #c5cbf1, #dadcf5, #eeedfa, #ffffff);
            width: 98vw;
            height: 98vh;
        }

        .flex-container {
            display: flex;
            width: 100%;
            height: 85%;
            flex-flow: column nowrap;
            justify-content: center;
            align-items: center;
        }

        .rename {
            width: 35%;
            height: 35%;
            background-color: #111111;
            border-radius: 50px;
            align-items: center;
            justify-content: center;
            align-items: center;
        }

        .rename > h1, .rename > h2, .rename > h3, h3 {
            font-family: 'Montserrat', sans-serif;
            text-align: center;
            color: #1f74cc;
            line-height: 40%;
        }

        .rename > form > label {
            display: flex;
            justify-content: center;
            color: #1f74cc;
            font-family: 'Montserrat', sans-serif;
        }

        input {
            height: 20%;
            border-radius: 40px;
            border: none;
            display: flex;
            justify-self: center;
            margin: auto;
            font-size: 100%;
            padding-left: 2%;
            margin-top: 1%;
            margin-bottom: 1%;
        }

       form {
            width: 100%;
            height: 50%;
        }

        button {
            margin: auto;
            height: 20%;
            width: auto;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #1f74cc;
            border-radius: 20px;
            font-size: 20px;
            color: white;
            border: none;
            transition-duration: 0.4s;
        }

        button:hover {
            height: 21%;
            width: auto + 1%;
            color:#1f74cc;
            background-color: white;
        }

        .footer {
            width: 40%;
            height: 5%;
            background-color: black;
            border-radius: 50px;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: auto;
            color: white;
            font-family: 'Montserrat', sans-serif;
        }

        a:link, a:visited, a:hover, a:active {
            color: #1f74cc;
            text-decoration: none;
            background-color: transparent;
        }

        @media screen and (max-width: 426px) {
            .rename {
                width: 70%;
                height: 25%;
            }

            .footer {
                width: 100%;
                font-size: 12px;
            }
        }
    </style>
    <div class="flex-container">
        <div class="rename">
            <h1><%= cloudname %></h1>
            <h2 class="subtitle"><%= lang["Rename"] %> <%= file %> </h2>
            <% let renameurl =  "/rename/" + file + (`/${folder}` || "" )%> 
            <form action=<%= renameurl %> method="post">
                <input name="_csrf" value=<%= csrfToken %> type="hidden"/>
                <label for="newname"><i class="fa-solid fa-pen-to-square"></i>&nbsp;<%= lang["New-Name"] %></label>
                <input type="text", id="newname", name="newname" required></input>
                <button type="submit" id="rename"><i class="fa-solid fa-pen-to-square"></i>&nbsp;<%= lang["Rename"] %></button>
            </form>
        </div>
    </div>
    <div class="footer">
        <p><%= cloudname %> ©️ 2022 - 2023 | Developed by <a href="https://github.com/DaneeSkripter">DaneeSkripter</a></p>
    </div>
</body>
</html>